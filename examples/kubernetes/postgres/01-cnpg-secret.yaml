# Dev-only credentials. Change before any non-local use.
apiVersion: v1
kind: Secret
metadata:
  name: buildkit-db-credentials
  namespace: buildkit
type: kubernetes.io/basic-auth
stringData:
  username: buildkit
  password: buildkit-secret
